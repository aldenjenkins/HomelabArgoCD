apiVersion: v1
kind: Service
metadata:
  name: reloader-reloader
  labels:
    app: reloader-reloader
spec:
  ports:
    - name: http
      port: 9090
      targetPort: 9090
      protocol: TCP
  selector:
    app: reloader-reloader
