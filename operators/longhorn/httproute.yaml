apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: longhorn-httproute
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: default-https-gateway
      namespace: istio-system
  hostnames:
    - longhorn.home.alden.ai
  rules:
  - backendRefs:
    - name: longhorn-frontend
      port: 80
      group: ''
      kind: Service
    matches:
    - path:
        type: PathPrefix
        value: /
