apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-rbac-cm
  namespace: argocd
data:
  policy.csv: |
    g, alden, role:admin
    g, readonly, role:readonly
  policy.default: role:readonly
  scopes: '[groups, email]'
