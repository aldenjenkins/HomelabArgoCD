apiVersion: v1
kind: Service
metadata:
  name: twentycrm-server
spec:
  ports:
    - name: http-tcp
      port: 3000
      protocol: TCP
      targetPort: 3000
  selector:
    app: twentycrm-server
  type: ClusterIP
