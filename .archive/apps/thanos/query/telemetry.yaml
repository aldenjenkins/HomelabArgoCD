apiVersion: telemetry.istio.io/v1
kind: Telemetry
metadata:
  name: disable-query-access-logs
spec:
  selector:
    matchLabels:
      app.kubernetes.io/component: query-layer
      app.kubernetes.io/instance: thanos-query
      app.kubernetes.io/name: thanos-query
  
  accessLogging:
    - disabled: true
