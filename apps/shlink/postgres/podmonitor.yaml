apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: shlink-postgres-18
  labels:
    app.kubernetes.io/name: postgresql
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: shlink-postgres-18
      cnpg.io/podRole: instance
  podMetricsEndpoints:
    - port: metrics
  namespaceSelector:
    matchNames:
      - shlink
