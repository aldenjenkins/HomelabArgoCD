apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: shlink-postgres
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  datasource:
    name: shlink-postgres
    type: postgres
    isDefault: false
    url: shlink-postgres-18-ro.shlink.svc:5432
    user: $username
    access: proxy
    jsonData:
      database: shlink
      postgresVersion: 1800
      sslmode: disable
      timescaledb: false
    secureJsonData:
      password: $password
  valuesFrom:
    - targetPath: secureJsonData.password
      valueFrom:
        secretKeyRef:
          name: shlink-postgres-18-app
          key: password
    - targetPath: user
      valueFrom:
        secretKeyRef:
          name: shlink-postgres-18-app
          key: username
