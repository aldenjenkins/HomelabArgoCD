namespace: grafana
resources:
  - namespace.yaml
  - configmap.yaml
  - postgres
  - redis
  - grafana.yaml
  - grafanafolder.yaml
  - grafanadashboard.yaml
  - dashboards
  - httproute.yaml
  - servicemonitor.yaml
  - probe.yaml
  # TODO: This prometheus rule is currently failing to evaluatle. the runbook url is 404 too, so maybe it needs updating.
  # - prometheusrule.yaml
  - aws
generators:
  - sopsgenerator.yaml
