apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaFolder
metadata:
  name: dynamodb
spec:
  uid: dynamodb-uid
  parentFolderUID: aws-uid
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
