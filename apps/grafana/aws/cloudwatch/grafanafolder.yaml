apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaFolder
metadata:
  name: cloudwatch
spec:
  uid: cloudwatch-uid
  parentFolderUID: aws-uid
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
