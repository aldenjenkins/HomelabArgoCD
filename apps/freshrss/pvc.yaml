apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: freshrss
  name: freshrss
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  selector:
    matchLabels:
      name: freshrss
  volumeName: freshrss
