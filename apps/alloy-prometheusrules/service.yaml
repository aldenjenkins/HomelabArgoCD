apiVersion: v1
kind: Service
metadata:
  name: alloy-prometheusrules
spec:
  selector:
    app: alloy-prometheusrules
  type: ClusterIP
  ports:
    - name: http
      protocol: TCP
      port: 12345
      targetPort: http
