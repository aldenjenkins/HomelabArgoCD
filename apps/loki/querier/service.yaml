apiVersion: v1
kind: Service
metadata:
  name: loki-querier
  labels:
    app.kubernetes.io/name: loki
    app.kubernetes.io/instance: loki
    app.kubernetes.io/component: querier
spec:
  selector:
    app.kubernetes.io/name: loki
    app.kubernetes.io/component: querier
  ports:
    - name: http-metrics
      port: 3100
      protocol: TCP
      targetPort: http
    - name: grpc
      port: 9095
      protocol: TCP
      targetPort: grpc
