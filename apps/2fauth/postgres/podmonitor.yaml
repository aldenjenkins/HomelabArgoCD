apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: 2fauth-postgres-18
  labels:
    app.kubernetes.io/name: postgresql
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: twofauth-postgres-18
      cnpg.io/podRole: instance
  podMetricsEndpoints:
    - port: metrics
  namespaceSelector:
    matchNames:
      - 2fauth
