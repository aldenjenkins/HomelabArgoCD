apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: 2fauth-postgres-18
spec:
  schedule: "0 10 0 * * *"
  cluster:
    name: twofauth-postgres-18
  backupOwnerReference: self
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
