apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: syncthing-nas-httproute
spec:
  parentRefs:
    - name: default-https-gateway
      namespace: istio-system
  hostnames:
    - syncthing.home.alden.ai
  rules:
  - backendRefs:
    - name: syncthing-nas
      port: 8384
    # matches:
    # - path:
    #     type: PathPrefix
    #     value: /
