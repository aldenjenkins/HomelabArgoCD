apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: backstage-postgres
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  datasource:
    name: backstage-postgres
    type: postgres
    isDefault: false
    url: backstage-postgres-18-ro.backstage.svc:5432
    user: $username
    access: proxy
    jsonData:
      database: backstage
      postgresVersion: 1800
      sslmode: disable
      timescaledb: false
    secureJsonData:
      password: $password
  valuesFrom:
    - targetPath: secureJsonData.password
      valueFrom:
        secretKeyRef:
          name: backstage-postgres-18-app
          key: password
    - targetPath: user
      valueFrom:
        secretKeyRef:
          name: backstage-postgres-18-app
          key: username
