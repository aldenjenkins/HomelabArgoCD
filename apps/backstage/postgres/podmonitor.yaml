apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: backstage-postgres-18
  labels:
    app.kubernetes.io/name: postgresql
spec:
  selector:
    matchLabels:
      cnpg.io/cluster: backstage-postgres-18
      cnpg.io/podRole: instance
  podMetricsEndpoints:
    - port: metrics
  namespaceSelector:
    matchNames:
      - backstage
