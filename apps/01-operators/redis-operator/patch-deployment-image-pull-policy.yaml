apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-operator-redis-operator
  namespace: redis-operator-system
spec:
  template:
    spec:
      containers:
      - name: manager
        imagePullPolicy: Always
