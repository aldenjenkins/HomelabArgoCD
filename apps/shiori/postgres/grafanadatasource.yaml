apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: shiori-postgres
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  datasource:
    name: shiori-postgres
    type: postgres
    isDefault: false
    url: shiori-postgres-18-ro.shiori.svc:5432
    user: $username
    access: proxy
    jsonData:
      database: shiori
      postgresVersion: 1800
      sslmode: disable
      timescaledb: false
    secureJsonData:
      password: $password
  valuesFrom:
    - targetPath: secureJsonData.password
      valueFrom:
        secretKeyRef:
          name: shiori-postgres-18-app
          key: password
    - targetPath: user
      valueFrom:
        secretKeyRef:
          name: shiori-postgres-18-app
          key: username
