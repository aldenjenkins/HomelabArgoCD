namespace: kube-system
resources:
  # - namespace.yaml
  # - github.com/kubernetes-sigs/descheduler/kubernetes/job?ref=release-1.34
  - descheduler.yaml

configMapGenerator:
  - files:
      - files/policy.yaml
    name: descheduler-config
    options:
      immutable: true
