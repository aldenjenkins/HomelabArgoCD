apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    nfs.io/storage-path: "ntfy/"
  labels:
    app: ntfy
  name: ntfy-data
spec:
  storageClassName: nfs-appdata-retain
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
