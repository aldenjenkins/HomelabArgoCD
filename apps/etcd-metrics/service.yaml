apiVersion: v1
kind: Service
metadata:
  name: etcd-metrics
  labels:
    app: etcd-metrics
spec:
  clusterIP: None
  ports:
    - name: metrics
      port: 2381
      targetPort: 2381
      protocol: TCP
