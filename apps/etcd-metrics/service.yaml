apiVersion: v1
kind: Service
metadata:
  name: etcd-metrics
  labels:
    app: etcd
spec:
  ports:
    - name: metrics
      port: 2381
      protocol: TCP
      targetPort: 2381
  selector:
    component: etcd
