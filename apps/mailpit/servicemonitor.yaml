apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: mailpit-metrics
spec:
  namespaceSelector:
    matchNames:
      - mailpit
  selector:
    matchLabels:
      app.kubernetes.io/name: mailpit
  endpoints:
    - port: http
