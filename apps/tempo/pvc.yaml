apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tempo-data
  annotations:
    nfs.io/storage-path: "tempo/"
  labels:
    app: tempo
spec:
  storageClassName: nfs-monitoring-retain
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
