namespace: tempo
resources:
  - namespace.yaml
  - serviceaccount.yaml
  - gateway
  - gossipring
  - memcached
  - metricsgenerator
  - ingester
  - distributor
  - compactor
  - querier
  - query-frontend
  - grafanadatasource.yaml
  - probe.yaml
  - prometheusrule.yaml
  - dashboards
configMapGenerator:
- files:
  - files/gateway/nginx.conf
  name: tempo-gateway
  options:
    immutable: true
- files:
  - files/tempo.yaml
  - files/tempo-query.yaml
  name: tempo-config
  options:
    immutable: true
- files:
  - files/overrides.yaml
  name: tempo-runtime
  options:
    immutable: true
