# Source: tempo-distributed/templates/backend-scheduler/service-backend-scheduler.yaml
apiVersion: v1
kind: Service
metadata:
  name: tempo-backend-scheduler
  namespace: tempo
  labels:
    helm.sh/chart: tempo-distributed-2.23.3
    app.kubernetes.io/name: tempo
    app.kubernetes.io/instance: tempo
    app.kubernetes.io/component: backend-scheduler
    app.kubernetes.io/version: "2.10.5"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: http-metrics
      port: 3200
      targetPort: http-metrics
      protocol: TCP
    - name: grpc
      port: 9095
      targetPort: 9095
  selector:
    app.kubernetes.io/name: tempo
    app.kubernetes.io/instance: tempo
    app.kubernetes.io/component: backend-scheduler
  ipFamilies: [IPv4]
  ipFamilyPolicy: SingleStack

