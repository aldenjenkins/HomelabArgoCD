apiVersion: v1
kind: Service
metadata:
  name: docker-registry
  labels:
    app: registry
spec:
  selector:
    app: registry
  ports:
    - name: registry
      port: 5000
      targetPort: 5000
    - name: metrics
      port: 5001
      targetPort: 5001
  type: ClusterIP
