namespace: docker-registry
resources:
  - namespace.yaml
  - redis
  - deployment.yaml
  - service.yaml
  - httproute.yaml
  - grafanafolder.yaml
  - grafanadashboard.yaml
  - servicemonitor.yaml
  - probe.yaml
configMapGenerator:
  - name: docker-registry-config
    files:
      - files/config.yml
    options:
      immutable: true
generators:
  - sopsgenerator.yaml
