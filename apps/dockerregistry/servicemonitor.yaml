apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: docker-registry
  labels:
    app: registry
spec:
  selector:
    matchLabels:
      app: registry
  endpoints:
    - port: metrics
      interval: 30s
