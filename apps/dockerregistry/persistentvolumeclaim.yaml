apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  annotations:
    nfs.io/storage-path: "registry/"
  labels:
    app: registry
  name: registry-data
spec:
  storageClassName: nfs-appdata-retain
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
