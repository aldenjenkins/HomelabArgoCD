apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: authelia-postgres
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  datasource:
    name: authelia-postgres
    type: postgres
    isDefault: false
    url: authelia-postgres-18-ro.authelia.svc:5432
    user: $username
    access: proxy
    jsonData:
      database: authelia
      postgresVersion: 1800
      sslmode: disable
      timescaledb: false
    secureJsonData:
      password: $password
  valuesFrom:
    - targetPath: secureJsonData.password
      valueFrom:
        secretKeyRef:
          name: authelia-postgres-18-app
          key: password
    - targetPath: user
      valueFrom:
        secretKeyRef:
          name: authelia-postgres-18-app
          key: username
