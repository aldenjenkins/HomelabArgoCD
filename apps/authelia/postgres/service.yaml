apiVersion: v1
kind: Service
metadata:
  name: authelia-postgres-healthz
spec:
  type: ClusterIP
  ports:
    - name: healthz
      port: 8000
      protocol: TCP
      targetPort: 8000
  selector:
    cnpg.io/cluster: authelia-postgres-18
    cnpg.io/instanceRole: primary
