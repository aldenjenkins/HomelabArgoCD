apiVersion: gateway.networking.k8s.io/v1alpha2
kind: TCPRoute
metadata:
  name: forgejo-ssh
spec:
  parentRefs:
  - name: default-https-gateway
    namespace: istio-system
    sectionName: forgejo-ssh  # References the listener name
  rules:
  - backendRefs:
    - name: forgejo-ssh
      port: 22
