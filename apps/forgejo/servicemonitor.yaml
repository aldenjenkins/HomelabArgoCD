apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app.kubernetes.io/name: forgejo
  name: forgejo
spec:
  endpoints:
    - path: /metrics
      port: http
      interval: 30s
  selector:
    matchLabels:
      app.kubernetes.io/name: forgejo
      app.kubernetes.io/component: http
