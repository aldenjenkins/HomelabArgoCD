apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: forgejo-data
  name: forgejo-data
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: longhorn
  resources:
    requests:
      storage: 100Gi
