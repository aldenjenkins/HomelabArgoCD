apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: forgejo-postgres
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  datasource:
    name: forgejo-postgres
    type: postgres
    isDefault: false
    url: forgejo-postgres-18-ro.forgejo.svc:5432
    user: $username
    access: proxy
    jsonData:
      database: forgejo
      postgresVersion: 1800
      sslmode: disable
      timescaledb: false
    secureJsonData:
      password: $password
  valuesFrom:
    - targetPath: secureJsonData.password
      valueFrom:
        secretKeyRef:
          name: forgejo-postgres-18-app
          key: password
    - targetPath: user
      valueFrom:
        secretKeyRef:
          name: forgejo-postgres-18-app
          key: username
