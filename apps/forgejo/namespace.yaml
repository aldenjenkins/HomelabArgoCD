apiVersion: v1
kind: Namespace
metadata:
  name: forgejo
  labels:
    enable-reloader-operator-watching: "true"
  # labels:
  #   istio.io/dataplane-mode: ambient
  #   istio.io/use-waypoint: shared-waypoint
  #   istio.io/use-waypoint-namespace: istio-system
