apiVersion: metallb.io/v1beta1
kind: BGPAdvertisement
metadata:
  name: bgpadv
  namespace: metallb-system
# spec:
#   ipAddressPools:
#     - bgp-service-pool
#   # Add this to ensure all nodes advertise (default behavior, but be explicit)
#   peers:
#     - router-peers
