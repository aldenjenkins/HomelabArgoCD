apiVersion: metallb.io/v1beta1
kind: IPAddressPool
metadata:
    name: bgp-service-pool
    namespace: metallb-system
spec:
    addresses:
        # MUST be a different subnet than your nodes' LAN IPs (e.g., not 192.168.1.x)
        - 10.10.10.10-10.10.10.11
    # autoAssign: true
