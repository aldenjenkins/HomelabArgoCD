apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: home-wildcard
  namespace: istio-system
spec:
  secretName: home-cert-tls
  issuerRef:
    name: letsencrypt-dns
    kind: ClusterIssuer
  commonName: "*.home.alden.ai"
  dnsNames:
    - "*.home.alden.ai"
    - "home.alden.ai"
