apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: immich-postgres
spec:
  allowCrossNamespaceImport: true
  instanceSelector:
    matchLabels:
      dashboards: "grafana"
  datasource:
    name: immich-postgres
    type: postgres
    isDefault: false
    url: immich-postgres-18-ro.immich.svc:5432
    user: $username
    access: proxy
    jsonData:
      database: immich
      postgresVersion: 1800
      sslmode: disable
      timescaledb: false
    secureJsonData:
      password: $password
  valuesFrom:
    - targetPath: secureJsonData.password
      valueFrom:
        secretKeyRef:
          name: immich-postgres-18-app
          key: password
    - targetPath: user
      valueFrom:
        secretKeyRef:
          name: immich-postgres-18-app
          key: username
