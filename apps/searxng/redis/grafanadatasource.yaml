apiVersion: grafana.integreatly.org/v1beta1
kind: GrafanaDatasource
metadata:
  name: searxng-redis
spec:
  instanceSelector:
    matchLabels:
      dashboards: grafana
  allowCrossNamespaceImport: true
  datasource:
    access: proxy
    jsonData:
      client: standalone
      poolSize: 5
      timeout: 10
      pingInterval: 0
      pipelineWindow: 0
    name: searxng-redis
    type: redis-datasource
    url: redis://searxng-redis.searxng.svc.cluster.local:6379
  plugins:
    - name: redis-datasource
      version: latest
