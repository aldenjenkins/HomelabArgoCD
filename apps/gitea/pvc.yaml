apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: gitea-data
  name: gitea-data
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  selector:
    matchLabels:
      name: gitea-data
  volumeName: gitea-data
