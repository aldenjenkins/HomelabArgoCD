apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  labels:
    app.kubernetes.io/name: gitea
  name: gitea
spec:
  endpoints:
    - path: /metrics
      port: http
      interval: 30s
  selector:
    matchLabels:
      app.kubernetes.io/name: gitea
      app.kubernetes.io/component: http
