apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: gitea-cluster-pre-upgrade-backup-2
  namespace: gitea
spec:
  cluster:
    name: gitea-postgres-18
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
# apiVersion: postgresql.cnpg.io/v1
# kind: Backup
# metadata:
#   name: gitea-cluster-pre-upgrade-backup
# spec:
#   cluster:
#     name: gitea-postgres-18
#   method: plugin
#   pluginConfiguration:
#     name: barman-cloud.cloudnative-pg.io
