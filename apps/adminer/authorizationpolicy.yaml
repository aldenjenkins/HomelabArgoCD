---
apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: adminer
spec:
  action: CUSTOM
  selector:
    matchLabels:
      app: adminer
  provider:
    name:  authelia
  rules:
    - to:
        - operation:
            hosts:
              - adminer.home.alden.ai
